CREATE TABLE IF NOT EXISTS TestModel (
    columnA VARCHAR(255),
    columnB DECIMAL,
    columnC VARCHAR(255),
    date TIMESTAMP,
    testEnum INTEGER,
    testFormatClass VARCHAR(255),
    booleanValue BOOLEAN
);